**№1:**  
`111`  
*Объяснение:* Локальная переменная `$aaa = 222` внутри функции не влияет на глобальную `$aaa = 111`.

**№2:**  
`111`  
*Объяснение:* Операция `$aaa++` внутри функции применяется к локальной переменной, которая не инициализирована и не связана с глобальной `$aaa = 111`.

**№3:**  
`222`  
*Объяснение:* Функция возвращает значение своей локальной переменной `$aaa = 222`, которое затем выводится.