**№1 Результат:**  
На экране ничего не выведется (пусто) или будет ошибка `Undefined variable $aaa`, так как переменная `$aaa` определена только внутри функции и не доступна в глобальной области.

**№2 Результат:**  
На экране ничего не выведется (пусто) или будет ошибка `Undefined variable $aaa`, так как параметр функции `$aaa` является локальной переменной и не связан с глобальной областью.