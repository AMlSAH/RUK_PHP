**№1 Ответ:** `3`

**Объяснение:** Функция возвращает значение параметра `$num` при первом операторе `return`, а последующий код не выполняется.

**№2 Ответ:** `1005`

**Объяснение:**
- `func(10)` → `10 > 0` → возвращает `10 * 10 = 100`
- `func(-5)` → `-5 <= 0` → возвращает `abs(-5) = 5`

**№3 Ответ:** `1005`

**Объяснение:** Логика идентична предыдущей задаче, так как при срабатывании условия в `if` функция завершается через `return`, иначе выполняется следующий за условием код.