**№1 Результат:** `a`  
Переменная `$aaa` передаётся в функцию по значению, изменение `$bbb` внутри функции не влияет на `$aaa`.

**№2 Результат:** Ошибка `Undefined variable $bbb`  
Переменная `$bbb` определена только внутри функции и недоступна в глобальной области.

**№3 Результат:** `a`  
Аналогично задаче №1: передача параметра по значению, изменение локальной переменной `$str` не затрагивает глобальную.

**№4 Результат:** `array(5) { [0]=> int(1) [1]=> int(2) [2]=> int(3) [3]=> int(4) [4]=> int(5) }`  
Массив передаётся по значению (копируется), поэтому изменения внутри функции не отражаются на исходном массиве.